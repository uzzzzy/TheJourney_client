const navbar = './navbar/'
const form = './form/'

export const Logo = require('./logo.svg').default
export const LogoDark = require('./logo-dark.svg').default

// icon
export const IconUser = require(navbar + 'profile.svg').default
export const IconWrite = require(navbar + 'write.svg').default
export const IconBookmark = require(navbar + 'bookmark.svg').default
export const IconBookmarkFill = require(navbar + 'bookmark-fill.svg').default
export const IconLogout = require(navbar + 'logout.svg').default

// bg
export const FormLeft = require(form + 'left.svg').default
export const FormRight = require(form + 'right.svg').default
